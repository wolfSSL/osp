Build CyaSSL v2.2.2 with the commands:

    $ ./configure --enable-fortress --enable-ocsp
    $ make
    $ sudo make install

Build FreeRADIUS with the commands:

    $ ./configure --with-cyassl --without-openssl --with-system-libtool --with-system-libltdl
    $ make
    $ sudo make install
